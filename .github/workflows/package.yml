name: Package

on:
    workflow_dispatch:

permissions:
    contents: read

jobs:
    manual-packaging-note:
        runs-on: ubuntu-latest
        steps:
            - name: Manual packaging
              run: echo "This fork publishes Windows and macOS builds manually from verified local artifacts."
